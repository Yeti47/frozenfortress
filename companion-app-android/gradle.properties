android.useAndroidX=true
# AGP 9's built-in Kotlin compiler - true is the AGP 9.0.1 default, set explicitly
# so the module doesn't silently start applying the standalone kotlin-android
# plugin again on an AGP downgrade.
android.builtInKotlin=true
org.gradle.jvmargs=-Xmx2048m
